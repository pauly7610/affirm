providers = ["python"]

[phases.setup]
aptPkgs = []

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && PYTHONPATH=/app/backend:/app/packages uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
